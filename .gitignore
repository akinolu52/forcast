# Raw data cache — regenerated by scripts/fetch_data.py.
# Root-scoped so it doesn't shadow docs/data/, which is committed.
/data/

# docs/data/*.json is generated by scripts/build_elo.py — checked in so
# the live site always has something to render, then overwritten by the
# M4 cron. Nothing to ignore here.

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
